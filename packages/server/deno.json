{
  "tasks": {
    // --allow-ffi is included; review if it's strictly necessary for server operations.
    "dev": "deno run --allow-net --allow-env=PORT --allow-read --allow-ffi --watch src/main.ts",
    // --unstable is used for tests, likely for features from @std/testing or other dependencies.
    "test": "deno test --allow-read --allow-env=PORT --unstable src/**/*.test.ts"
  }
}
